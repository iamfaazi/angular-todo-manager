export enum Priority {
  Low = 'Low',
  Medium = 'Medium',
  High = 'High',
}

export enum Status {
  NotDone = 'not done',
  Done = 'done',
}

export enum SortBy {
  Priority = 'priority',
  Status = 'status',
}
